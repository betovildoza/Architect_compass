"""tree-sitter scanner (Tier 2) — placeholder.

TODO (SCN-003, NIVEL 4 del PLAN.md):
    Scanner genérico que recibe una grammar como parámetro desde
    language_grammars del config (CFG-005). Un único módulo cubre PHP, JS, TS,
    Ruby, Go, etc.

    Opt-in: si tree-sitter no está instalado, los lenguajes cubiertos caen
    automáticamente a regex_fallback.
"""
